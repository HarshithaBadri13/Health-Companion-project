# Rakshak Health Demo

Minimal Vite React scaffold created from the original `app.html`.

Install dependencies and run locally:

```bash
npm install
npm run dev
```

Open http://localhost:5173 in your browser.
